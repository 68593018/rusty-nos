[package]
name = "nos-common"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
ipnet = "2.9"
async-trait = "0.1" # ✅ 新增：支持异步 Trait

